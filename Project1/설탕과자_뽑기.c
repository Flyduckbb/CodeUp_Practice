#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int h, w, cnt;
	int l, d, x, y;
	int arry[100][100];

	scanf("%d %d", &h, &w);
	scanf("%d", &cnt);
	scanf("%d %d %d %d", &l, &d, &x, &y);

	for (int i = 0; i < cnt; i++)
	{
		
	}

}